namespace FrederikaStudio.GLFW;

public sealed class Keyboard
{
    //public static int GetKeyScancode(_nGLFW.Keys key) => Library.Native.GetKeyScancode(key);
    //public static string GetKeyName(_nGLFW.Keys key, int scancode) => Library.Native.GetKeyName(key, scancode);

    //private _nGLFW.Window Handle { get; }

    //internal Keyboard(Window window)
    //{
    //    Handle = window.Handle;
    //}

    //public _nGLFW.InputState GetKey(_nGLFW.Keys key) => Library.Native.GetKey(Handle, key);
}
